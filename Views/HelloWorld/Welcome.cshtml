@{
    ViewData["Title"] = "Bem Vindo";
}

<h2>Bem Vindo</h2>

<ul>
    @for (int i = 0; i < (int)ViewData["NumTimes"]; i++)
    {
        <li>
            @ViewData["Message"]
        </li>
    }
</ul>